<div id="bookmark" style="max-height:100px;overflow:auto;">
	<%= form_tag('/admin/topics/bookmark', :remote => true, :id => "bookmark_form") do %>
		<table>
			<tr>
				<th>Time</th>
				<th> Bookmark </th>
				<th></th>
			</tr>
			<% video = topic.videos.first %>
			<%= hidden_field_tag 'id', video.id %>
			<% if video.bookmark.nil? %>
				<tr>
			  	<td>
			  		<input type="text" name="bookmark[]time" class="form-control" value="0.00">
			  	</td>
			  	<td>
			  		<input type="text" name="bookmark[]description" class="form-control">
			  	</td>
			  	<td>
			  		<a onclick="$(this).parent().parent().remove();javascript:void(0)" class="link">Remove</a>
			  	</td>
			  </tr>
			<% else %>
				<% JSON.parse(video.bookmark).each do |bookmark| %>
					<tr>
				  	<td>
				  		<input type="text" name="bookmark[]time" class="form-control" value="<%= bookmark['time'] %>">
				  	</td>
				  	<td>
				  		<input type="text" name="bookmark[]description" class="form-control" value="<%= bookmark['description'] %>">
				  	</td>
				  	<td>
				  		<a onclick="$(this).parent().parent().remove();javascript:void(0)" class="link">Remove</a>
				  	</td>
				  </tr>
				<% end %>
			<% end %>
		</table>
	<% end %>
</div>