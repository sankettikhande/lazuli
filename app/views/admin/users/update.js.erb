<% if @user.errors.blank? %>  
	window.location.href = "<%= admin_users_url %>"  
<% else %>
	$('#error_messages').addClass('error_message_tag').html('<%= error_message_tag(@user.errors.full_messages) %>')	
	$(window).spin('stop')
<% end %>
