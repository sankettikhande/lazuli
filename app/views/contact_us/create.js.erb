<% if @contact_us.errors.blank? %> 
  	window.location.href = "<%= root_url %>"  
<% else %>   
	$('.error').addClass('error_message_tag').show().html('<%= error_message_tag(@contact_us.errors.full_messages) %>')
	$(window).spin('stop')
<% end %>