<div class="lazuli-container">
  <%= render 'shared/shared_header'%>
  <%= render 'shared/user_actions' %>
	<% if @favourite_videos.any? %>
		<%= form_for @favourite_videos, :url => remove_favourites_url, :remote => true, :method => :post, :html =>{ :id => "remove_videos" }  do |f| %>
			<%= render 'shared/shared', :objekts => @favourite_videos %>
		<% end %>
	<% end %>
</div> <!-- container ends -->
