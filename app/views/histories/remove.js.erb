<% if @videos %>
	$("#remove_videos").html("<%= j(render 'shared/shared', :objects => @videos, :type => 'history_ids') %>");
<% else %>
	$("#remove_videos").html("No videos are found in History.");
<% end %>