<%= render 'shared/shared_header'%>
<%= render 'shared/user_actions' %>
<% if @videos.any? %>
  <%= form_for @videos, :url => url, :remote => true, :method => :post, :html =>{ :id => "remove_videos" }  do |f| %>
		<%= render 'shared/shared', :objekts => @videos %>
	<% end %>
<% else %>
	<label>No <%= params[:controller].titleize %> found.</label>
<% end %>