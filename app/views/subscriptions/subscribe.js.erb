$("#modal-window").show();
$("#modal-window").html("<%= escape_javascript(render 'shared/subscribe', :course => @course, :course_subscriptions => @course_subscriptions, :current_subscription => @current_subscription) %>");
$(".subscription_now").trigger('click');