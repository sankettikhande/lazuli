<% if @user_review.errors.blank? %> 
  	window.location.href = "<%= root_url %>"  
<% else %>   
	$('.error').addClass('error_message_tag').show().html('<%= error_message_tag(@user_review.errors.full_messages) %>')
	$(window).spin('stop')
<% end %>