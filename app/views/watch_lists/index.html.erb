<div class="lazuli-container">
  <%= render 'shared/shared_header'%>
  <%= render 'shared/user_actions' %>
  <% if @videos.any? %>
	  <%= form_for @videos, :url => remove_watch_lists_path, :remote => true, :method => :post, :html =>{ :id => "remove_videos" }  do |f| %>
			<%= render 'shared/shared', :objects => @videos, :type => "watch_list_ids" %>
		<% end %>
	<% end %>
</div> <!-- container ends -->
<%= javascript_include_tag "application/main_app/user_option.js"%>